# BanG Dream 組牌檢索

Weiss Schwarz BanG Dream 卡表篩選（效果綁定、CX コンボ、助太刀、對手倒置等）。

**網址（給朋友點這個）：**  
https://cinccino-with-kingsrock.github.io/bd-ws-search/

本機預覽：把 `web/` 裡的 `index.html` 用瀏覽器打開即可。更新卡表後執行 `python tag_cards.py`。
